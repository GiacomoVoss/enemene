export enum ParameterType {
    REQUEST = "REQUEST",
    RESPONSE = "RESPONSE",
    PATH = "PATH",
    QUERY = "QUERY",
    BODY = "BODY",
    CURRENT_USER = "CURRENT_USER",
    CONTEXT = "CONTEXT",
}
