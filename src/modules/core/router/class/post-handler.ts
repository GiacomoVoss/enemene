export class PostHandler {

    // public static create<ENTITY extends DataObject<ENTITY>>(clazz: any, data: Dictionary<serializable>): PathRequestHandlerFunction<ENTITY> {
    //     return async (req: ISecureRequest, path: PathDefinition) => {
    //
    //         const object: ENTITY = await DataService.create(clazz, data);
    //         return await DataService.filterData(object, req, clazz.name, path.authorization);
    //     };
    // }
}
